# Cloudflare Workers Static Assets configuration.
# 把 Astro build 產出的 dist/ 直接當靜態網站部署，無 SSR / 無 Worker script。
name = "thermoacoustic-lab-site"
compatibility_date = "2024-12-01"

[assets]
directory = "./dist"
not_found_handling = "404-page"
